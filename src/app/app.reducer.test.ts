// import {setAppError, appSlice, setAppStatus, InitialStateType} from "./appSlice";
//
//
// let startState: InitialStateType;
//
// beforeEach(() => {
//     startState = {
//         error: null,
//         status: 'idle',
//         isInitialized: false
//     }
// })
//
// test('correct error message should be set', () => {
//
//     const endState = appSlice(startState, setAppError({error:'some error'}))
//
//     expect(endState.error).toBe('some error');
// })
//
// test('correct status should be set', () => {
//
//     const endState = appSlice(startState, setAppStatus({status:'loading'}))
//
//     expect(endState.status).toBe('loading');
// })


export {}